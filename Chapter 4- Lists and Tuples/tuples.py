#creating tuple using ()
t= (1,2,4, 5)
print(t[0])

#cannot update values from a tuple and that is the difference between tuple and list
t[0]=0 #throws an error

t1=() #empty tuple
t1= (1) # wrong way to denote single tuple
t1=(1,) #single element tuple

